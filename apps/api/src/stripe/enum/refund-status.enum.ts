export enum RefundRequestStatus {
  REQUESTED = 'pending',
  REJECTED = 'rejected',
  REFUNDED = 'approved',
}

export enum RefundStatus {
  PENDING = 'pending',
  CREATED = 'created',
  REFUNDED = 'refunded',
}
